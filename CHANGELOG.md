# Changelog

## 0.18.0 — 2026-09-10

**A cockpit could carry dates and prove nothing about them.** The roadmap held dates, `state.json` held phases, and nothing connected the two — so no verb could answer the question every operator eventually asks: *when does this end?* Answering it meant reading a roadmap by hand, counting logs by calendar date to derive a rate, and doing the arithmetic on paper, from inputs already sitting in `state.json` and in git. A downstream cockpit had already written `launch_date` and `feature_freeze` into its `state.json` as unschematized fields, tolerated by `casp check` and verified by nothing. Someone needed this and wrote it into the file anyway, where it rotted quietly. **A dated claim nobody confronts with the calendar does not go stale — it goes false.**

This release adds an opt-in `casp/schedule.json`, four rules that verify it, and one printer verb. It records and verifies dated claims; it never proposes one.

- **The boundary, decided before the first line: the clock may add a WARN and may never add a FAIL.** "No clock in the gate" was never the doctrine — `CASP-FACT-003` has read today's date since 0.11. The doctrine is a deterministic comparison of a recorded claim against a defined evidence source, and the calendar is one *when the user recorded the date*. The narrower, testable discipline kept from that debate: **the `CASP-SCHEDULE-*` family never changes the exit code because of the clock.** A passed date is a WARN. Lateness honestly recorded is the record being correct, not drift. Two tests pin it — severity (004 is WARN) and **clock invariance** (the same fixture checked a year apart yields an identical FAIL set). A future edit that inverts either has to break the suite loudly.
- **New file — `casp/schedule.json`** (`schemas/schedule.schema.json`), with `anchors` (fixed dates, an *ordered* array whose position IS the declared chronology — no `order` field, because a second declaration of the same thing is one more thing that can contradict itself) and `due` (a date on a phase). Dates are `YYYY-MM-DD` only: no time, no zone, no relative expression — a zone makes the same file resolve differently on two machines, which is the determinism bug in miniature. The join key is the **phase name**, the unit the three phase lists already verify, not the prompt path, which is one indirection away and renames more often. `note` is tolerated and read by nothing.
- **Opt-in is structural, not a promise in prose.** `templates/templates/schedule.json` ships as a copyable example under the scaffolds directory, so `casp init` copies it to `casp/templates/` and never to `casp/schedule.json`; `casp upgrade` refreshes the example and skips the live file as operator data. Both claims are asserted by tests rather than trusted. No `casp/schedule.json` ⇒ **zero** `CASP-SCHEDULE-*` findings, not even a PASS.
- **Four new rules, ninth area `SCHEDULE`.** `001` FAIL — the file does not validate (a malformed date, an unknown `before`, a duplicate anchor id, each reported as a sentence and not a stack). `002` **WARN** — a dated phase appears in no phase list; WARN on purpose, because phases get renamed mid-flight and failing the push for a rename teaches people to delete the schedule, which is the opposite of the goal. `003` FAIL — the schedule contradicts itself: anchors not strictly ascending, or a queued/backlog phase dated on or after the anchor it declares `before`. `004` WARN — a queued/backlog phase's date, or the anchor it is bound to, is in the past. Shipped phases are never inspected by `003` or `004`: **history is not drift.**
- **New verb — `casp schedule [--since <weeks>] [--plain] [--json]`,** a printer. Four blocks, and the order is the argument: the **measured pace** first, walked from `git log -- casp/state.json` by reading `phases_shipped.length` out of each commit's blob; the **derived length** of the queue (`phases_queued ÷ pace`) with the date it lands on; the **recorded claims** marked `ahead` / `due` / `passed`; the **contradictions** `casp check` would emit. A reader who sees the recorded date before the real rate reads the date as a plan. The window (8 weeks, `--since` overrides) and the exact commits used are printed next to the rate — averaging the whole history is wrong for any project whose pace changed, and every project's pace changes. Fewer than two usable data points, no span, nothing shipped in the window: one sentence saying so, no derived length, exit 0. **casp never invents a rate.** The pace is measured live every run and recorded nowhere: a stored pace drifts from the real one by construction, which is the defect the facts layer exists to catch.
- **Printing the derived date is deliberate.** Refusing to print it while printing the quotient is theatre — the reader adds it to today either way. What is refused is a number whose method is invisible, so the window, the commits and the arithmetic share the screen with it, labelled *at the measured pace — arithmetic, not a forecast*.
- **`casp status` draws the board.** A progress line, always, from the three phase lists: shipped fills the bar, queued is the empty part, **backlog is named but not drawn** — a backlog has no committed length. One schedule line when the file exists (the next unshipped claim, its date, its distance). `--json` is unchanged and the schema stays v1: no field earned its place.
- **`casp close` ends on the board.** It used to end on the bump and the check verdict. Nothing in the close protocol produced a picture of where the project stood, so no session ever showed one and the operator rebuilt it by hand from the logs. The verb every close already runs is where the picture belongs. Same renderers as `casp status` and `casp schedule`, never a second one. `close` still runs no git.
- **The drawings are pinned as exact strings** in both charsets (UTF-8 blocks, and `#`/`.`/`-` degraded — `--plain` or `CASP_ASCII=1`), because a drawing nobody pins drifts silently.
- **Ruled out, so they do not return as proposals:** no planning, no estimation, no work assigned to a week, no queue ordered by urgency, no pace recorded in the file. No second "done" register — that is `phases_shipped`, already verified against git. No PM surface: no decision gates, no hours, no capacity, no WIP limit, no assignees, no inter-item dependencies (`next_after` already encodes order and is already gated). No HTML, no Gantt page, no tracker artifact, no reminders, no daemon, no calendar import or sync — local, invoked, deterministic. A shareable page would be a harness artifact (the `fleet` precedent: distributed by casp is not part of casp) and is a skill in a later session, if anyone asks. **No automatic migration** of an unknown `launch_date` / `feature_freeze` found in a downstream `state.json`: reinterpreting a user's field as protocol is not a move this tool makes. No new dependency — date arithmetic is `Date.parse` on ISO strings.
- **231 → 251 tests.** (The 0.17.0 entry's "231 → 235" was measured wrong: `node --test` over the whole suite reports 231 on that commit's tree. The number above is that same command, before and after.)

## 0.17.0 — 2026-09-05

**A finished roadmap is not a finished project.** Until now a cockpit whose last slice had shipped could set `next_prompt: null` over an empty queue and pass the gate as "genuinely parked" — and the next session opened on a blank. The blank was a lie of omission: what follows an implemented roadmap is distribution, pricing, support, the next roadmap — each a **decision** before it is code, and a decision is a session too.

- **New rule — `CASP-PROMPT-011`, "a cockpit that has shipped never reports nothing to start".** FAIL when `phases_shipped` is non-empty, `phases_queued` is empty and `next_prompt` is empty. The exemption is deliberate and tested: a cockpit that has shipped nothing is *not started*, not *finished* — `casp init` stays green out of the box and the fresh-null-pointer case keeps passing. `CASP-PROMPT-005` (parked while queued) is unchanged.
- **New prompt kind — `kind: discussion`.** `casp new discussion --slug <slug>` scaffolds `DISCUSSION-<SLUG>.md` from a new template (`templates/templates/discussion-prompt.md`, delivered to existing cockpits by `casp upgrade`): a session with the human whose deliverable is **decisions written down** — one question, one recommendation each — and the prompts they produce. No code. The template names the four decisions every finished roadmap owes (next roadmap, distribution, pricing, support) and leaves room for the project's own.
- **`casp next` announces the kind.** On a `kind: discussion` head it prints, on stderr before the body, that the prompt needs the human and must not run headless. stdout is unchanged, so pipes keep working.
- **Schema descriptions** for `next_phase` / `next_prompt` no longer describe `null` as "parked (roadmap complete)": a complete roadmap is exactly when the pointer must not be empty.
- **Test rot fixed.** `casp fact list` asserted `fresh: true` on a fact hard-coded as verified on 2026-07-20 with a 30-day TTL; it started failing on 2026-08-20 on every clock. The fact is now verified "today". 231 → 235 tests.
- **Skills.** `/next` treats an empty pointer on a cockpit that has shipped as drift (`casp check`, then a discussion prompt), and runs a `kind: discussion` head as a conversation: positions first, decisions recorded, prompts drafted, no product code.

## 0.16.0 — 2026-08-17

**The `fleet` skill ships in the package — and is not CASP.** Parallel agent sessions on one repository are where state drift hurts most, so the package now distributes a fourth Claude Code skill, `skills/fleet`, alongside `/casp`, `/next` and `/audit-batch` — none of which are protocol verbs either. The boundary is stated everywhere the skill is mentioned, and each claim is testable: **fleet launches sessions, therefore it orchestrates, therefore it is never a CASP feature**; no `casp check` rule reads it and nothing in it gates a push; and **its model default is empty** — the reasoning ships (a controller and its workers do not need to run at the same tier), a model name never does, because naming one contradicts a model-agnostic tool and dates the file at the next release.

- **New — `skills/fleet/SKILL.md`.** Written from three measured trials on three separate repositories, not from intentions. What the trials established, now stated in the skill: the default shape is **one writer plus N adversarial read-only reviewers** (a reader holds no lane; in that shape the concurrent-write family is empty by construction); **the measured value is contradiction, not speed** — no speed gain was demonstrated and none is claimed; whether a project's gates are **isolable per session** (ports, database, fixtures) is a per-project property to measure before launching — one trial repository forbade parallelism and failed silently, another allowed it except for one build command and failed loudly, naming the shared directory, and the readability of the failure is what separates a constraint from a trap; the dominant failure mode in every trial was a **stale belief** about shared state, mitigated by a fetch-and-report at open and before the final report; and commits go **by pathspec**, never `-a`, never `git add -A`, because the git index is shared by every process in the repository.
- **Launcher decision: skill only, no launch script ships.** The working launcher this skill was distilled from is OS- and terminal-emulator-specific; porting it would put session-launching machinery inside the very package whose README says CASP never launches anything, and a documented launcher contract with no implementation would be speculative API surface. The skill states the two invariants any environment must honour — each worker starts with its brief already loaded, under a stable addressable name — and leaves the opening of sessions to the user's environment.
- **Wiring path claims into a launcher is ruled out, not pending.** Three trials produced 14 recorded incidents: a `casp live` path claim would have caught **1** and would have refused **2** writes on entirely legitimate paths. The guard is imprecise in both directions, and the collisions that actually occur travel through the git index, `HEAD`, the working tree, a dev-server port, a lockfile, a build cache — none of which is a file-writing tool call, so no path claim sees them. The effective mitigations are procedural and are in the skill. Do not re-propose the wiring without a measurement that overturns that count.
- **One new test file** (222 → 228 tests): the shipped skill is mechanically checked for zero absolute paths, zero model names, zero repository names other than this package, and zero speed claims — the same greps used as the acceptance gate for this release.

## 0.15.1 — 2026-08-16

**Failing open is the contract. Failing open in silence is not.** 0.15.0's dormancy rule is correct and stays exactly as it is — reserved paths arm only while a controller and another lane are both backed by a process this machine can probe. What was wrong is that a category which had NOT armed said nothing about it. A controller declared from a bare terminal takes the `cli:<user>` fallback identity and carries no process id, so it arms nothing; the only way to discover that was to notice that a guard which should have fired never did. **A protection believed to be on and actually off is worse than no protection, because it is trusted.** Reporting only — no verdict changes, no new subcommand, no rule added, and `casp check` still cannot see any of it.

- **`casp live claims` now states where the reserved category stands and WHY**, on its own line, in every state: `no controller declared` · `the controller row carries NO PROCESS ID, so it arms nothing — it was declared from a bare terminal rather than from inside the session` · `no other lane is held — a solo session is never blocked` · `no other lane carries a process id (N lane(s) bounded by TTL alone)` · or `ENFORCED`, with the reason it is armed. `--json` gains `reserved_paths: { armed, reason }`, and a kill switch reports `armed: false` rather than a category it is standing down. CASP records and reports; an unarmed guard is a fact of state, which is exactly its business.
- **Docs — the limit that matters most, named rather than papered over: `casp live` guards path WRITES, not the side effects of shared state.** A claim covers a path and the guard fires on a file-writing tool call against it. It has nothing to say about an action that stays inside its own lane and still reaches out through state the whole repository shares. The case, measured rather than imagined: `git add <one file> && git commit` published **945 lines of unrelated deletions** another session had staged, because `git commit` without a pathspec publishes the entire **index** — one object shared by every process in the repository. No claim would have prevented it and none should be expected to: the guard saw a `Bash` call, and `Bash` is deliberately unguarded. The index is not alone — a regenerated lockfile, a schema migration, a dev-server port, a build cache are all in-lane actions with out-of-lane effects. `docs/threat-model.md` now says so plainly, alongside the mitigations, which are procedural rather than mechanical. A tool that states precisely what it does not do is more credible than one that lets the first serious user find out.
- **One new regression test** (221 → 222): every unarmed state names itself, the armed state still reads as armed, and a kill switch never reports an armed category.

## 0.15.0 — 2026-08-16

**`casp live` — the in-flight record next to the durable one.** `state.json` syncs at session boundaries; between them, N parallel agent sessions (plus the human running them) coordinate by hand or collide. This release adds the ephemeral half: advisory path **claims** with a TTL and a holder-liveness check, an append-only **journal** every session feeds through its harness lifecycle hooks, and **`casp live watch`** — the human's whole-fleet live view. The design bet is stated up front: share the *state*, never the chat stream. Every message in a shared agent stream lands in N contexts and invalidates N prompt caches; a journal line costs one append, and the human — for whom reading is free — gets the entire stream anyway. Additive throughout: one new verb, no schema change, no existing finding changes verdict. 200 → 221 tests. This entry carries the **pre-publish adversarial review** folded in — 0.15.0 was never published, so its fixes land here rather than as a 0.15.1.

- **New — `casp live claim / release / claims`.** A claim holds a repo-relative path prefix for one session (identity: `CLAUDE_CODE_SESSION_ID`, `--session`, or `cli:<user>`), with a TTL (default 480 min) and the claiming harness PID when one exists. Overlap with a foreign claim is refused **in both directions** — claiming `src/` over someone's `src/app` is the same collision as the reverse — and matching is segment-boundary prefix, never glob: `src/app` does not capture `src/apples.ts`, and the whole scheme stays explainable in one sentence.
- **New — `casp live hook`, one command wired for every harness event.** As a `PreToolUse` hook it is the guard: a file-writing tool call (`Edit`/`Write`/`NotebookEdit`) on a path covered by an **active, living** foreign claim exits 2 with the owner named on stderr — the one blocking case in the whole verb. As `PostToolUse`/`SessionStart`/`Stop`/`SubagentStop` it journals `edit`/lifecycle events. `casp live install` prints the settings block; it deliberately never writes anyone's settings file.
- **Fail-open is the contract, not a fallback.** A tool designed to coordinate must never be able to forbid work: an expired claim, a claim whose holder process is gone (`kill(pid, 0)` → ESRCH), a corrupt claims file, malformed hook stdin, an unknown event, any internal error — all of it means *no coordination*, never *no editing*. Pruning is lazy, on read; there is no daemon. `Bash` is deliberately unguarded: parsing arbitrary shell for file targets is guesswork, and a documented blind spot beats a guessing gate. Same posture for symlinked roots — the comparison stands down (ALLOW) rather than police a path it cannot normalize.
- **Kill switches that require no file edit inside the wedged session.** A misfiring guard blocks file writes — so the escape hatch cannot be "edit settings.json". `CASP_LIVE=0` (checked on every hook invocation), `casp live off` (repo marker), `casp live off --global` (`~/.casp/live.off`, machine-wide — a globally installed hook has a machine-sized blast radius, so the off ramp is machine-sized too).
- **Ownership has three states, not two — and the third is the one that removes most collisions before any tooling.** `owned by X` (a claim; a fleet lane is claims declared at launch, so conflicts surface at launch, not at the 400th write), `controller-only`, and `free`. The controller-only category covers shared state no lane may own — the cockpit, session logs, root instruction files, lockfiles — written solely by the session wearing the controller hat (`casp live controller`). The list is **configurable** (`casp/live.config.json` `{ "reserved": [...] }`, full-replace) with sensible defaults; entries are root-relative prefixes or exact basenames, still no globs. And the rule the whole design hangs on: **the controller hat is dormant while the session is alone.** Reserved paths are enforced only when a controller is declared AND at least one other lane is held, **and both rows are backed by a process this machine can probe** — a guard that blocked `casp/state.json` for a solo session would break the tool's primary use, so arming it takes evidence, not merely an unexpired timestamp. The controller row obeys the same TTL + liveness pruning as any claim: a dead controller disarms the category rather than locking everyone out.
- **The boundary with the gate is enforced by the filesystem, not by discipline.** `casp/live/` self-writes a `.gitignore` on first touch: runtime churn never reaches `git status`, therefore never reaches `casp check`. Live never gates a push; check never reads live. No LLM, no network, no telemetry — a poll of a local file is the whole "real-time" machinery.
- **Pre-publish adversarial review — two ways the fail-open contract did not hold, each reproduced before it was touched.** The review's brief was the contract itself: enumerate every path through the hook and prove each degraded state exits 0. Two did not.
  - **A claim whose `expires_at` could not be parsed never expired.** Expiry was `expires_at > nowISO()` — a **string** comparison, exactly right for timestamps this binary wrote and fail-**closed** for every one it did not. `"not-a-date"`, an empty string, a `+14:00` offset spelling an instant two hours in the past: each sorts *above* the current UTC string, so each read as valid forever. Combined with a row carrying no PID — the rows with no liveness evidence at all — a corrupt claims file blocked its path permanently, on a verb whose stated contract is that a corrupt claims file must not wedge the repo. Expiry is now parsed (`Date.parse`); unparseable reads as **expired**, the ALLOW direction.
  - **Two stale rows could lock a later solo session out of its own cockpit.** Reserved enforcement armed on "a controller row and one foreign lane row, both un-expired". Rows with `pid: null` are bounded only by the 480-minute default TTL, so a controller and a worker left behind by two sessions that died hours earlier armed the category against a **brand-new solo session** and refused its writes to `casp/state.json` until the TTL ran out — the number-one trap of the three-state model, arriving through the back door. Arming now requires both rows to be **PID-backed and probed alive**. A PID-less row still binds its own path (advisory, as claimed); what it may no longer do is extend the reserved category over a third party's shared state on no evidence. The honest cost: a harness exporting no process id gets claims without reserved enforcement. When the evidence is absent the answer is ALLOW, every time.
- **Four more from the same pass.** **The hook stopped being a writer** — pruning persisted the pruned file on every read, which made `PreToolUse` (one run per tool call, across N sessions) a writer of `claims.json` with no compare-and-swap; the interleaving that matters is a prune holding a pre-claim snapshot landing *after* a fresh `claim`, silently dropping a claim that was reported taken. It did not reproduce in 12 forced trials — the window is one write per pruning event — and the cure is free: prune in memory, let the mutating commands persist it. **The journal rolls** at 5 MB into `journal.1.jsonl` instead of growing without bound; `watch` already treated a size drop as a rotation. **`casp` and `session-logs` became explicit `casp/` and `session-logs/` prefixes** in the default reserved list — as bare basenames they reserved `bin/casp`, and a guard that blocks on a name collision is a guard people turn off. **A subagent is no longer denied its parent's lane**: ownership is the session id, or failing that the same harness process id, since a harness is free to hand a subagent its own `session_id` and the resulting denial would be a block — the one direction this design may not fail in.
- **Six new regression tests** (215 → 221), each pinning a reproduced case: four unparseable or offset-form `expires_at` values that must not block / two stale PID-less rows that must not arm reserved paths against a solo session, while still binding their own path / a subagent sharing its parent's harness process that must not be denied, while a genuinely foreign session still is / the hook leaving `claims.json` byte-identical / `casp live claims` announcing a standing-down kill switch on its first line and restoring the self-`.gitignore` if it was deleted by hand / the journal rolling at its ceiling. `casp live claims --json` gains an `enforcing` boolean, and `runLive` backstops any unforeseen throw under `hook` with exit 0.

## 0.14.2 — 2026-07-22

**Documentation only. No binary change since 0.14.1** — `dist/` is byte-identical, and the six README screenshots still show `casp` 0.14.1 output because 0.14.2 produces exactly the same output. This release exists to refresh the README on the npm package page, which only updates on publish; the rewrite below landed on GitHub after 0.14.1 was already out.

- **The README now leads with five screens of real output** before any prose: the gate blocking a lying push, the queue handing over the next slice and refusing one that already shipped, the plan checked as an executable plan, the one-screen status, and a declared number that stopped being verified. The full reference that used to be the whole document is unchanged, moved below. Screenshots are regenerated by a committed script (`docs/img/capture-readme-shots.sh`) that refuses to run unless the installed `casp` matches the release it documents.

## 0.14.1 — 2026-07-21

**Fixes an orphan storm on the single most common drift.** Found by dogfooding — capturing screenshots for the README on a real cockpit, which is the same way 0.13.0's chain defect surfaced. Report quality only: no verdict changes, no exit code changes, `check --json` stays schema **v1**, and no rule is added or removed. 193 → 195 tests.

- **FIX — `next_prompt` pointing at an already-shipped slice buried its own finding under one warning per queued prompt.** 0.13.0 shipped a guard for exactly this, and its comment in `src/chain.ts` says so: *"when next_prompt is missing or already shipped, CASP-PROMPT-001/003 already FAIL, and burying that one actionable finding under an orphan warning per queued prompt would make the report worse."* The condition implemented half of that sentence. `hasHead` tested `byRel.has(headRel)`, and `byRel` holds **every** prompt including shipped ones — so a head that existed but had already shipped passed the guard, the reachability walk ran from a node nothing chains forward from, and every declaring queued prompt came back unreachable. On a five-slice queue the report read `1 FAIL · 4 WARN` where it should read `1 FAIL`, and the four were noise about a plan that is perfectly coherent: it is the head that is wrong, not the order behind it. "Usable head" now means a prompt that can still **run** — present *and* `queued`/`in-progress` — not merely a file that exists.
- **Why the existing test missed it.** There *was* a test for the guard: `no usable head → no orphan storm on top of the real finding`. It pointed `next_prompt` at `GONE.md` — a head that does not exist. The other half of the documented sentence, the head that exists but already shipped, was never exercised. A test that pins half a claim reads exactly like a test that pins all of it.
- **Two new regression tests.** A shipped head produces `CASP-PROMPT-003` and **zero** `prompt_chain.orphan.*` findings; and `casp status --json` reports `queue: null` on a shipped head rather than a partial order a consumer cannot tell is partial — the same principle 0.13.0's post-review fix established when a false `queue` shipped.
- **New — `docs/img/capture-readme-shots.sh`.** The README's six proof screenshots are regenerated from a real cockpit by a committed script, and it **refuses to run unless `casp --version` matches the release it documents**. A screenshot taken on an older binary is a stale claim inside the one document that argues stale claims are the problem.

## 0.14.0 — 2026-07-21

**The facts layer — proving freshness, never truth.** Everything `casp check` has ever verified is a claim *inside* `state.json` against git. It has never had an opinion about the documents *orbiting* the cockpit — a unit-cost note, a roadmap line, a summary percentage — because none of those is a state claim. A real incident showed the cost of that gap: five false claims lived in exactly those documents on one working day, and `casp check` stayed green throughout, correctly, because none of them was drift. This release closes that gap without crossing the one line the product refuses to cross: no LLM, ever, in the binary. Additive and opt-in throughout: no existing finding changes verdict, `check --json` stays schema **v1**, and 193 tests are green (151 → 193). This entry also carries the **hostile-filesystem hardening** folded in before publish — 0.14.0 was never published, so it lands here rather than as a 0.14.1.

- **New — `casp/facts.json`, opt-in, same posture as `CASP-SESSION-003` and the prompt-chain rules.** A cockpit that never scaffolds it sees **zero** `CASP-FACT-*` findings — not even a PASS. A fact declares a claimed `value`, the `source` it was derived from (a repo path, or `external:<label>` for evidence outside the repository), a `source_hash` taken at verification time, a reproduction `method`, `verified_at` + `ttl_days`, and optionally which documents (`used_in`) cite it.
- **The reversal that makes this tractable without a model: CASP cannot prove a claim is *true* — it can prove a claim has stopped being *verified*.** Has the source changed since verification (hash)? Has the verification aged past its declared shelf life (TTL)? Was a reproduction method ever recorded? Three comparisons, zero LLM — the same shape `migrations_applied` already uses.
- **New — `CASP-FACT-001` … `CASP-FACT-006`.** `001` the declared source resolves (a repo path, or a labeled `external:`) — **FAIL**. `002` `source_hash` matches the source's current content — **FAIL**, and the one rule that would have caught the founding incident's core case: a cost value derived from a config file, never recalculated after the file changed under it. `003` the fact has not exceeded `ttl_days` — **WARN**, **FAIL** past double the TTL; every fact must declare a TTL, since an `external:` source has no hash to fall back on. `004` a `used_in` document carries a `<!-- casp:fact <id> -->` marker — **WARN**, presence-only, never the prose around it (comparing a number in natural language needs a model). `005` a reproduction `method` is recorded — **WARN**. `006` `method` matches no entry in a static trap registry (`src/traps.ts`) — **FAIL**, and the rule that would have caught a PostgreSQL planner row-count estimate (`n_live_tup`) read as an exact count, off by roughly 40x in the founding incident.
- **The trap registry is data, not a model.** `n_live_tup`/`n_dead_tup` without `count(`, `EXPLAIN` without `ANALYZE`, `reltuples`, `docker stats --no-stream` — four cataloged shapes that produce an estimate reading like a measurement. Extensible per-project via a plain `traps` string array in `facts.json`, matched as a **substring only**, never a regex — facts.json is repository content, and executing an arbitrary pattern from it would be the same mistake this layer exists to avoid making with a model.
- **New — `casp fact list|check|verify|stale`.** `list`/`check`/`stale` are read-only; `check` is the FACT-only subset of `casp check`, `stale` is the re-verify work list. `verify <id>` is the **one deliberate code-execution surface in the binary**: it replays the fact's declared `method`, shows the before/after (`value`, `source_hash`, `verified_at`), and writes the result. It asks **twice**, and the order is the security property: `run this command?` **before** anything executes, then `write this fact?` once the before/after is on screen. In a non-interactive shell it refuses and exits 1 rather than assuming consent; `--yes` is the only bypass and skips both. Nothing else in CASP ever executes repository content, and no gating path — `check`, the pre-push hook, `next`, `status`, `--all` — can reach it.
- **Fix (caught pre-publish) — the confirmation gated the write, not the execution.** The first cut of `verify` ran `method` and *then* asked `write this fact?`. That reads as consent but grants none: `casp fact verify <id>` executed arbitrary shell out of `casp/facts.json` — repository content, possibly written by an agent or arriving with a cloned repo — with no TTY, no `--yes` and nobody asked. It even printed `refusing to write without confirmation` **after** the command had already run. Reproduced before it was touched; the confirmation now precedes execution and the non-interactive path refuses before running. `docs/threat-model.md` claimed "no arbitrary code execution to verify state… by construction" throughout, which was false for one verb; it now scopes that promise to the gating paths and documents this surface, its consent rules, and the fact that it is a consent gate rather than a sandbox. Also: `Ctrl+D` at either prompt now aborts cleanly instead of printing an `AbortError` stack trace over the question.
- **Why the existing test did not catch it.** There *was* a test for the non-interactive path — it asserted that nothing was **written**. Nothing asserted that nothing was **run**, so it passed with the hole wide open. The two new tests assert the method's *side effect*, which is the invariant that actually matters. A test pinning the wrong invariant is indistinguishable from no test.
- **Fix — compare-and-swap on `state.json` writes, the incident's other half.** `saveState` became atomic (temp file + `rename`) in 0.12.1, which protects a *partial* write (a crash, a full disk) but nothing protects an *overwritten* one. Two agents writing the same `casp/` in parallel is an observed failure mode, not a hypothetical: the implicit model until now was one agent, one session, one branch. `loadStateWithHash` now remembers the hash of what it read; `saveState` re-hashes the file right before the `rename` and refuses on any mismatch — no lock, no merge, an honest refusal and nothing written. Wired through the four verbs that mutate state: `close`, `ship`, `audit bump`, `upgrade`.
- **`docs/what-casp-proves.md` gets a sharper limit, in its own voice.** The facts layer proves freshness, never truth: an unchanged source and an unexpired TTL can both be right about freshness and wrong about substance if the original verification was itself mistaken. The trap registry catches only the traps it knows — the founding incident's error was a judgment error, not a freshness error, and the next unknown measurement trap will still pass silently. `CASP-FACT-005` does not prevent that; it makes it auditable after the fact, which the incident had none of.
- **Hostile filesystem hardening — a gate that crashes is not a verdict (folded in pre-publish).** `existsSync` answers "is there a name here", never "can this process open it". A file that existed and could not be **read** — mode `000`, a directory squatting a `*.md` path, a symlink cycle — threw straight out of whichever verb touched it. Reproduced against the built binary on a tree with one `chmod 000` prompt: `casp check` printed a raw Node `EACCES` stack trace; **`casp check --json` and `casp status --json` printed nothing at all** while exiting 1. The `status --json` row is the serious one — `docs/status-json.md` documents that verb as never gating (`exit 0` for any valid cockpit, drift included), so an agent got a non-zero exit and an empty stdout from a contract that says neither can happen, with no way to tell "the cockpit is broken" from "casp is broken". Same class as the 0.13.0 non-string `next_prompt` crash: a machine contract that silently does not hold.
- **One door, not scattered `try`/`catch`.** Every read of repository content now goes through `readTextFile` / `readBytes` / `readDirEntries` / `readFrontmatter` / `readStateFile` in `src/shared.ts`, which return a discriminated result — content, or a reason (`unreadable` / `is-directory` / `vanished`) carrying the path and the operating system's own errno. Patching each call site would have worked exactly once; a single door is what stops the next reader reintroducing it. `readFrontmatter`'s signature changed accordingly, which is what keeps a caller from silently conflating "cannot open" with "no frontmatter" — the distinction the old `null` return threw away.
- **New — `CASP-IO-001` (a file the gate had to read could not be opened) and `CASP-IO-002` (the validation run did not complete).** Both **FAIL**: an unverifiable claim is not a passing claim. The gate is allowed to say *I could not check this*; it is not allowed to say *clean*. Each finding names the path **and** the reason — `prompt is unreadable (EACCES) · docs/plan/sessions/PHASE-1-FIRST-SLICE.md` — because neither alone is actionable. Fixing this by swallowing the error into an exit 0 would have turned a loud crash into a silent pass, which is strictly worse than the bug.
- **`check --json` always emits a valid report, and `status --json` honours its documented exit contract.** `checkOneSafe` guarantees a parseable v1 document even if the validator itself fails; `schema_version` stays **1** with no field renamed or retyped. `status` and `doctor` keep their non-gating exit codes on a hostile tree. A top-level handler in `src/cli.ts` backstops anything unforeseen with a one-line diagnostic and exit 1 — a backstop, not the fix: the fix is that the read paths no longer throw.
- **One deliberate behaviour change.** A directory named `*.md` inside the sessions or logs directory used to be *silently skipped*; it is now a `CASP-IO-001` FAIL. A document CASP enumerated and could not read is exactly how a broken state surface reads as clean — the same false-green pattern already refused for a missing migrations directory. A repo with such a directory was green and is now red; that is the intent.
- **The commissioned audit caught a regression the fix itself introduced, before push — a `*.md` FIFO made the gate HANG.** Removing the old `isFile()` filter handed non-regular files to `readFileSync`, and `readFileSync` on a pipe with no writer does not fail, it **blocks in `open(2)` forever**. No `try`/`catch` catches a hang. A gate that never returns is strictly worse than the stack trace it replaced: no verdict *and* no exit code, so a pre-push hook wedges the terminal and CI runs out to its own timeout. Reads now `stat` before they open, and a pipe, socket or device named like a document is a `CASP-IO-001` finding. Three further fixes from the same review: an unreadable `next_prompt` living inside the sessions directory was reported **twice** with one id (a consumer keying by id would silently drop one, and the drift count read 2 for one defect) — findings are now deduplicated by path, most specific label winning; a file that *vanished* mid-walk was silently skipped, which could empty the declared-phase set and take the entire `CASP-SESSION-003` category down with it, converting a real FAIL into no output at all — a race, but exactly the false-green class this phase exists to remove, so `vanished` is no longer exempt; and `status --json`'s assembly outside `checkOneSafe` could still throw before its single write, reproducing the original defect one function over — it now always emits a valid v1 document and exit 0.
- **Fourteen new regression tests** (193 total), one per reproduced case, asserting observable behaviour and **the absence of a stack trace on stderr**: an unreadable prompt FAILs `check` with `CASP-IO-001` / `check --json` still parses and carries the finding at `schema_version` 1 / `status --json` parses and exits **0** / `doctor` exits 0 / `next` refuses in one line with no prompt on stdout / a directory named `*.md` in the sessions dir is a finding and the report is still complete / an unsearchable sessions directory is a finding / a genuinely *missing* prompt stays `CASP-PROMPT-001` and is not reclassified as an IO failure / a healthy repo emits no `CASP-IO-*` finding at all / both new codes appear in `casp rules` and resolve in `casp explain` / an unreadable `casp/state.json` reports as unreadable rather than as the flatly wrong "not valid JSON". They skip cleanly under uid 0, since `chmod 000` does not deny root. The genuine TOCTOU — a file unlinked between the existence check and the open — is **not** deterministically reproducible from outside the process and is stated as such in the test file rather than faked with a sleep.
- **Twenty-eight regression tests for the facts layer** (179 at the time): a cockpit without `facts.json` sees literally nothing / a malformed `facts.json` is one FAIL, not a crash / an unresolvable source and a label-less `external:` both FAIL / a source changed after verification FAILs and a re-verify clears it / a repo-relative source with no recorded hash FAILs / TTL WARNs then FAILs past double / an `external:` fact with no `ttl_days` FAILs / a `used_in` document with and without the marker WARN and PASS respectively / the built-in trap registry FAILs a known bad pattern and PASSes a real measurement / a project-declared trap in `facts.json` also FAILs / `list`, `check`, `stale` and `verify` each round-trip correctly, including refusing to write without confirmation in a non-interactive shell / **the method does not execute without consent** (the side effect is asserted absent, not just the state file) / `--yes` still runs it / the method is printed before the gate / `saveState`'s compare-and-swap refuses a stale write, leaves the winning write untouched, and cleans up its own temp file on refusal.

## 0.13.0 — 2026-07-21

A new **drift category** for the half of the protocol that had no gate behind it: the queue. Everything before this validated the *head* of the queue (`state.next_prompt`) and the integrity of what had already *shipped* — nothing validated the ordering of what had not run yet. `next_after` was a template convention with no rule behind it. Four codes now prove the plan itself is executable. Additive and backward-compatible: no existing finding changes verdict, the `check --json` schema stays **v1**, no new state key, and every prior test stays green (121 → 151).

- **New — `CASP-PROMPT-007` … `CASP-PROMPT-010`, prompt-chain integrity.** The documented workflow is to draft a run of prompts ahead of time, chain them with `next_after`, point `next_prompt` at the head, and execute one per session without re-planning. That workflow is only as good as the chain, and the chain could be silently incoherent while `casp check` stayed green. Now: `007` — a `next_after` naming a slice that resolves to nothing (**FAIL**); `008` — a cycle, including a prompt naming itself (**FAIL**); `009` — two queued prompts claiming the same predecessor (**WARN**); `010` — a queued prompt no chain from the head reaches (**WARN**).
- **The severity split is deliberate.** A dangling reference and a cycle are claims that *cannot be true* — the plan is unexecutable as written, so they gate the push. A fork or an orphan is *ambiguous* rather than false, and a deliberate parking lot of queued-but-unchained prompts is a legitimate way to work; reddening it would punish a real workflow.
- **It cannot redden a repo that has not opted in.** The canonical template ships `next_after: <previous-session-id-or-prompt-slug>` as a literal placeholder, so a large share of real prompts in the wild carry an unedited value. A placeholder, an empty string and `null` are **not declarations** and produce nothing. A repo where no queued prompt declares a real predecessor gets **no finding at all** — silence, the same treatment `CASP-SESSION-003` gives a repo where no log declares a phase. Adoption is derived from the data: no new state key, nothing to configure. Prompts skipped as non-declaring are counted in a scope line, never silently.
- **Resolution is exact, never fuzzy.** A `next_after` resolves against evidence the validator already reads: a prompt in the sessions directory (by filename stem, its lowercase form, or its slug with the scaffold's `PHASE-` prefix removed), a session id that maps to a log (the `CASP-SESSION-001` resolver), or a phase id declared by state. Every match is an exact string after a documented normalization — `CASP-SESSION-003` refused fuzzy filename matching and so does this. A near miss is a dangling reference, not a resolved one.
- **Identity collisions resolve by rule, not by `readdir` order.** `A.md` and `PHASE-A.md` both answer to the slug `a`. Identities are ranked most-specific-first (stem, lowercase stem, slug) and inserted least-specific-first over a path-sorted list, so an exact stem always wins and the resolver is a pure function of the filenames — the same repo cannot resolve differently on two filesystems.
- **History is not blamed.** Only *queued* prompts are subjects. A shipped prompt is a valid resolution target — a chain legitimately terminates on the slice that ran before it — but its own `next_after` is never re-litigated.
- **No orphan storm on top of a real finding.** When `next_prompt` is missing or already shipped, `CASP-PROMPT-001` / `003` already FAIL; the reachability walk stays silent there rather than burying the one actionable finding under a warning per queued prompt.
- **`casp status --json` gains `queue`** — the resolved chain as repo-relative prompt paths, head first, so an agent can plan more than one session ahead. `null` when the chain is not adopted or not coherent (no guessed order). Additive: the status schema stays **v1**, and `status` still exits `0` on a drifted chain — reporting, never gating.
- **FIX (pre-existing, found while adversarially probing this slice) — a non-string `next_prompt` crashed the whole gate.** `state.json` accepts any JSON, and `next_prompt: 42` (or a list, or an object) reached `join()` and threw `ERR_INVALID_ARG_TYPE`, killing the entire report with a raw stack trace instead of emitting a finding. A crashed gate is worse than a red one: CI reads it as infrastructure failure rather than as drift, and every other check in the run is lost with it. A non-path value is now a `CASP-PROMPT-001` FAIL naming the offending type. Same class as the scalar-`state.json` crash fixed in 0.12.1; `casp next` and `casp status` were already guarded, `check` was not.
- **Thirty new regression tests** (151 total), each pinning a reproduced case rather than the abstract rule: a never-adopting repo emits not even a PASS line / an unedited placeholder, `null` and `''` produce nothing / no non-string YAML value is ever treated as a declaration (number, float, boolean, list, map, `~`, whitespace-only) / a hostile-shaped `state.json` reports drift instead of crashing, for every container type / a coherent chain PASSes and states its skipped count / the slug, session-id and phase-id forms each resolve / a dangling reference FAILs and exits 1 / a near-miss filename is dangling, not fuzzy-matched / a two-node cycle is one finding, not one per node / a self-reference FAILs / a fork WARNs without blocking / an orphan WARNs without blocking / a parked prompt is never an orphan / a shipped prompt's dangling `next_after` is ignored while the prompt itself stays a valid target / a missing head produces no orphan storm / all four codes appear in `casp rules` and resolve in `casp explain` / every chain finding carries a rule code / `status --json` exposes the order, and `null` on a drifted or unadopted chain.
- **Post-assembly review fixes, folded in before publish.** A commissioned adversarial review of this slice returned after the first commit and found one real blocker plus three smaller defects; `0.13.0` was never published to npm, so all four are folded into this entry rather than a `0.13.1`.
  - **BLOCKER — an aliased fork went undetected, and `status --json` then published a false order.** Fork detection was keyed on the raw `next_after` string, the one site in the module that did not get the identity treatment. `next_after: PHASE-A` and `next_after: phase-a` name one slice, but looked like two different predecessors: the `CASP-PROMPT-009` WARN was missed, the chain was declared **coherent**, and the `queue` field then emitted a linear order asserting that the second prompt runs after the first — a machine-readable claim the frontmatter did not support. Worse than the missed warning, because a consumer has no way to tell. Fork detection now keys on the **resolved** target; every identity of a slice shares one canonical key.
  - Fork messages show **each claimant's own spelling** (`PHASE-B (as 'PHASE-A'), PHASE-C (as 'phase-a')`), so the report never prints a canonical name the operator never typed.
  - **A cycle no longer also emits an orphan WARN per ring member.** Ring members are unreachable *because* of the ring, which already FAILs; restating one defect N times is the noise the missing-head guard already refuses.
  - **The skipped count is stated even when the chain is not coherent.** It lived only in the PASS line, so it vanished exactly when the report was non-trivial — against the spec's "never silent". A scope line is now emitted either way.
- **Found by dogfooding, and it worked.** On its first real fixture — this repository — the rule immediately surfaced that the queue was not actually chained: two queued prompts named slices that had shipped long ago, so the plan's real order was ambiguous. The frontmatter is fixed in this release and the chain now resolves.

## 0.12.1 — 2026-07-21

**Fixes two data-loss paths in `casp upgrade` (0.12.0). Upgrade immediately; 0.12.0 is deprecated on npm.** Found by an adversarial review that reproduced both by execution, after 0.12.0 had already been published — the publish was premature and the fix is same-day.

- **FIX — a symlinked parent directory let a write escape the cockpit.** The symlink check inspected only the final path component, but every parent is followed by `mkdirSync`/`writeFileSync`. A symlinked `casp/templates/` — or a symlinked `casp/` itself, ordinary in a monorepo — meant `upgrade` wrote into, and overwrote files in, a directory it was never pointed at. No race was required; a plain run did it. Every write target's resolved parent must now sit inside the resolved cockpit, and an unresolvable path fails closed.
- **FIX — the refresh set was a denylist, so unknown files were overwritten by default.** It named `state.json`, `now.md` and `roadmap.md` and refreshed everything else the package ships. Any root-level template added in a future release would therefore have silently replaced whatever the operator had written at that path, with no warning line. It is now an **allowlist**: `README.md` plus everything under `casp/templates/`. A shipped file this code has not heard of is skipped, not written — the default for the unknown is "leave it alone".
- **FIX — an unrecognised flag was ignored, so a mistyped dry run performed a real write.** `casp upgrade --dryrun` (and `-N`) applied. On the one verb that writes into a user's repository, unrecognised now exits 2 without writing anything.
- **FIX — a `state.json` holding a bare scalar crashed the run.** `42` parses, is truthy and is not null, so it passed the guard and the property assignment threw under strict mode — after the scaffolds were written and before the version stamp, leaving a raw stack trace. A state file that is not an object is now reported as a broken cockpit and left alone.
- **FIX — `saveState` is atomic.** It was a naked truncating `writeFileSync`: a crash or a full disk mid-write destroyed the state file it was updating. Now a temp file plus `rename`, which is atomic within a filesystem. Pre-existing and shared, so this also hardens `ship`, `close` and `audit`.
- **Four new regression tests** (121 total), each pinning a reproduced exploit rather than the abstract rule: an escaping symlinked parent leaves the outside file byte-identical and creates nothing there; an unrecognised root-level file survives an upgrade; `--dryrun` / `-N` / `--force` each exit 2 and write nothing; a scalar `state.json` produces no `TypeError`.

## 0.12.0 — 2026-07-21

The protocol's own continuity across releases. `casp upgrade` closes the gap where CASP could improve its scaffolds but had no way to deliver them to a repo already on CASP. One new command makes it a minor bump; everything is additive — no existing finding changes verdict, `casp check` gains no rule, the `check --json` schema stays v1, and every prior test stays green (118 total).

- **New — `casp upgrade`, refresh a cockpit's scaffolds without eating its state.** Before this verb, `init` refused on an existing `casp/` and `init --force` overwrote *every* file the package ships under `templates/` — `state.json`, `now.md` and `roadmap.md` included. The only refresh path destroyed the operator's data. `upgrade` is the surgical alternative: it rewrites **only** `casp/README.md` and `casp/templates/**`, and it **never writes `now.md` or `roadmap.md` and never templates over `state.json`**. The single state write is additive — stamp `casp_version`, round-tripped through the parsed object so every existing value stays byte-identical. It never deletes (a scaffold the package stopped shipping is left alone), it is idempotent (a re-run on a current cockpit writes nothing and says so), and `--dry-run` / `-n` prints the same plan and writes nothing. No `casp/` → exit 1 pointing at `init`. `upgrade` never gates; `check` remains the only gate.
- **Why now.** It was a no-op by accident from 0.7.0 to 0.9.0, when zero scaffold bytes changed between releases. **0.11.0 ended that**: it shipped a new session-log template carrying the `phase:` frontmatter that `CASP-SESSION-003` reads — so the newest rule was unadoptable by every cockpit scaffolded before it, short of copying the file by hand or losing the state file. The refresh list is *derived* from what the package ships minus an explicit data denylist, so the next scaffold change is delivered without touching this code.
- **New (additive) — `casp_version` in `state.json`.** The CASP release that last scaffolded or upgraded the cockpit. `casp init` stamps it; `casp upgrade` refreshes it. **Optional and never gating**: a cockpit scaffolded before the stamp existed carries none and still PASSes `casp check` — asserted by a regression test, because a new required key would have reddened every existing user repo on upgrade. Documented in `schemas/state.schema.json`.
- **`casp doctor` — a cockpit-staleness `WARN`.** A new `cockpit.version` probe compares the stamp against the installed CLI: `PASS` when equal, `WARN` when the cockpit is older (pointing at `casp upgrade`), when it carries no stamp at all, or — the inverse anomaly — when it was stamped by a *newer* CASP than the one installed. `WARN`, never `FAIL`: doctor still always exits `0`.
- **Namespaced on purpose.** The stamp is `casp_version`, not `version`. `state.json` has always accepted arbitrary extra keys, so a bare `version` would have silently overwritten a project recording its *own* product version — in the one verb whose entire promise is that no existing value changes.
- **Hostile filesystem shapes are handled, not crashed on.** A **symlink** in the cockpit points somewhere the operator chose, possibly outside `casp/`; `upgrade` reports it as `symlink` and never writes through it. A **write that fails** (a directory in the way → `EISDIR`, an unwritable file → `EACCES`, a regular file where `casp/templates/` belongs → `ENOTDIR`) is reported per file and the run continues to the version stamp and to exit `0` — an abort there would have left the cockpit half-refreshed *and* unstamped, so every re-run would repeat the same partial write. A broken install with no packaged `templates/` says so instead of throwing an `ENOENT` stack.
- **`casp/README.md` is a scaffold, and the output says so.** It is refreshable by design, but it is also the one refreshable file an operator plausibly hand-edits, so its `refresh` line carries `(replaces your local edits — keep notes in now.md)`. The closing summary vouches only for the three data files, deliberately.
- **The scaffolded-on date is preserved.** `casp/README.md` records the date the cockpit was created — a fact about that repo, not a template value. `upgrade` reads the existing date back out and keeps it, so a README that differs from the package copy only by its date reports `same` and a run today and a run next month agree.
- **Also — a coverage test that was assumed to exist and did not.** Nothing asserted that the verbs `src/help.ts` documents and the verbs `src/cli.ts` dispatches are the same set; the hardcoded list in `test/help.test.mjs` had fallen five verbs behind without failing. The invariant is now checked structurally in both directions, so a verb can never ship dispatched-but-undocumented or documented-but-undispatched.
- **Sixteen new regression tests** (118 total): a stale cockpit is refreshed while `now.md` / `roadmap.md` / every non-stamp state value stay byte-identical / the README date survives a refresh / a second run writes nothing / `--dry-run` and `-n` write nothing and do not move the stamp / a missing scaffold directory is re-added with the 0.11.0 template / a symlinked scaffold is left alone and its out-of-cockpit target is never rewritten / a directory in the way is reported per file while the run still stamps and exits 0 / refreshing `README.md` warns that local edits are replaced / `README.md` is pinned as the only shipped non-data scaffold carrying `{{TODAY}}` (a tripwire: a future one would break idempotency silently) / no `casp/` exits 1 with the init pointer / `init` stamps the version and the fresh cockpit still checks green / an unstamped cockpit still PASSes `check` / doctor's four version verdicts, all exiting 0 / `upgrade` is a first-class verb in both the top-level deck and its own help block / an unrecognised flag never turns `upgrade` destructive / help ↔ dispatch coverage in both directions.

## 0.11.0 — 2026-07-21

First new **drift category** since the rule codes landed: `CASP-SESSION-003` ties the shipped scoreboard to the written record. A new rule can change a repo's verdict, so the whole design is built so it cannot redden a repo that has not opted in — every prior test stays green (102 total), the `check --json` schema stays v1, and no existing finding changes verdict.

- **New — `CASP-SESSION-003`, every shipped phase must be declared by a session log.** A `phases_shipped[]` entry is a claim that a slice shipped; this rule requires a session log to say so. Severity **FAIL** with a `→ fix` hint, so it gates the push like any other drift.
- **The mapping is declared, never inferred.** A log opts in with a `phase:` key in its YAML frontmatter — a scalar id, or a list when one session shipped several. **Filenames are never consulted**: a log named `26-07-19-001-0-10-0-audit-watermark.md` against the phase id `0.10.0-audit-watermark` would need exactly the fuzzy match this category refuses to make. If the mapping would need a guess, there is no finding.
- **Adoption is derived from the data, not configured.** No new state key. `phases_shipped` is ordered and append-only, so the first entry any log declares opens the enforcement window; every entry from there on must be declared, and everything before it is exempt as pre-adoption (the exempt count is printed, never silent). A repo where no log declares a phase gets **no finding at all** — silence, the same treatment migrations get in a project that tracks none. Backfilling an old log only moves the window earlier; it never fabricates history.
- **Retroactive adoption, without lying.** A repo adopting CASP with years of history behind it is not asked to invent logs it never wrote. Two honest paths: declare from the next session on (history stays exempt), or backfill `phase:` into the old logs that do exist — which pulls the window earlier and enforces everything after it. Removing an entry from `phases_shipped` is the third: a scoreboard that claims less is not drift.
- **Template — `casp new log` now scaffolds `phase:`** in the session-log frontmatter, with the placeholder documented inline. An unedited placeholder declares a phase that `phases_shipped` does not contain, so the window stays closed and the check stays silent until adoption actually anchors.
- **Scope, stated honestly.** This catches *the scoreboard claiming more than the record supports*. It does **not** catch shipping without recording — a release that never gets appended to `phases_shipped` leaves nothing for this rule to test. That gap is the queued `facts-layer` phase's job, not this one's.
- **Ten new regression tests** (102 total): never-adopted repo stays silent / fully declared passes / an undeclared shipped phase FAILs and exits 1 / retroactive adoption exempts pre-adoption history and names the window / a gap after adoption still FAILs while history is not retroactively blamed / one log declaring a list satisfies each entry / a declared phase absent from `phases_shipped` keeps the window closed (guards the `findIndex` → `slice(-1)` trap) / a suggestively-named log without `phase:` does not count / a directory named `*.md` in the logs dir is skipped instead of crashing the report with `EISDIR` / non-string `phase:` values (number, bool, null, nested map) are ignored rather than declared.

## 0.10.0 — 2026-07-19

Two-tier verification pass. One new command (`audit`) makes it a minor bump; everything is additive and backward-compatible — no existing finding changes verdict, `casp check` does not gain a rule, the `check --json` schema stays v1, and every prior test stays green (92 total).

- **New — `casp audit`, the deep-audit watermark.** CASP now names the boundary between two verification tiers that were being conflated. The **cheap per-merge tier** — `fmt` + typecheck + lint + the touched module's own tests — is gated by `casp check` and runs every session; it is the only thing standing between a refactor and an irreversible bug (money, cross-tenant). The **expensive holistic tier** — an adversarial sub-agent audit + the full e2e battery + a security review — is slow and token-heavy, so it runs in **batch, on demand, over everything merged since the last one**, never per session. `casp audit status` prints the unaudited range `last_deep_audit..HEAD` (commit + file counts, `--json` for data); `casp audit bump [<sha>]` records HEAD (or a given commit) as deep-audited. A new optional `last_deep_audit` state field (short SHA) holds the watermark. **It is a production-cutover gate, never a merge gate: `casp check` does not block on it** — a stale watermark delays a deploy, it never blocks a commit. `bump` resolves its ref through injection-safe git (`gitArgs`) and refuses a non-commit; `status` treats a rebased-away watermark as orphaned and asks for a re-baseline. Managed end-to-end by the new `/audit-batch` skill (`skills/audit-batch`), which scopes the review from `audit status`, runs the battery + auditor once over the range diff, and bumps the watermark only on GO.
- **Motivation.** Running the holistic pass every session was turning a ~7-minute close into ~40 minutes of largely duplicated or batchable work (a full local `verify` CI already re-runs, a token-heavy sub-agent audit, a full e2e battery). Separating the tiers keeps the irreversible-bug gate on every merge while letting the semantic/security/e2e risk accumulate safely behind an explicit, queryable watermark that must clear before a cutover.
- **Schema — `last_deep_audit` documented** in `schemas/state.schema.json` (optional; a project that never runs the batch pass never sets it). Fully additive.
- **Five new regression tests** (92 total): status with no watermark (whole tree unaudited) / bump writes HEAD and status then reports up to date / status counts commits + files since the watermark / bump on a bogus ref exits 1 and leaves state untouched / unknown subcommand exits 1.

## 0.9.0 — 2026-07-15

DX + machine-handoff pass. Two new commands (`doctor`, `version`) make it a minor bump; everything is additive and backward-compatible — no existing finding changes verdict, the `check --json` schema stays v1, and every prior test stays green.

- **New — `casp doctor`, a read-only environment diagnostic.** Answers "is this machine set up to run casp?" — not "has the state drifted?" (that stays `casp check`'s exclusive job). It probes the **environment**: Node version (>= 20), the `git` binary and repository, `casp/state.json` presence + validity, the resolved `sessions_dir` / `logs_dir`, `core.hooksPath`, and whether a CASP-managed pre-push hook is installed. Prints `PASS` / `WARN` / `FAIL` per line, plus `--json` (own `schema_version`). **It never gates — always exits `0`, even on a `FAIL`**: a map of what to fix, not a gate. It reuses the same directory resolver and hook-detection as the rest of the binary, so its verdicts never diverge from `check` / `install-hook`. Documented in [docs/doctor.md](docs/doctor.md).
- **New — `casp version` + `casp version --json`.** The plain form prints the version (byte-identical to `casp -V` / `casp --version`, which are unchanged). `--json` emits the machine handoff `{ name, version, node, schema_version }`, where `schema_version` is the `check --json` report schema version — so an agent can negotiate the check-report shape from one call. `name` and `version` are read from `package.json` (single source of truth, via a shared reader).
- **New (additive) — structured `expected` / `actual` on `check --json` findings.** Where a finding has a single natural expected-vs-actual pair — `last_commit.git` (HEAD vs the recorded sha), `next_prompt.status` (`queued` vs `shipped`), `migrations.match` (on-disk vs in-state) — the JSON finding now carries `expected` / `actual` so a consumer can diff without regex-parsing `detail`. Both are `null` on every other finding. **Fully additive: `schema_version` stays `1`**, no field renamed or removed, finding order unchanged, and the human report is byte-identical (the fields are JSON-only). The published `check-result.schema.json` and [docs/check-json.md](docs/check-json.md) are updated in lockstep.
- Thirteen new regression tests (87 total): doctor healthy / missing-state (FAIL but exit 0) / invalid-JSON / `--json` envelope + summary tally / hook WARN→PASS after `install-hook` / outside-a-repo WARN; `version` plain == `-V` and `--json` shape with matching `schema_version`; `expected` / `actual` populated on the shipped-prompt and unresolvable-`last_commit` drifts and `null` on passes.

## 0.8.0 — 2026-07-14

Protocol-maturity + hardening pass. New commands (`rules`, `explain`) make it a minor bump; everything else is additive and backward-compatible — no existing finding changes verdict, the `check --json` schema stays v1, and every prior test stays green.

- **New — stable rule codes on every finding, plus `casp rules` / `casp explain`.** Each `casp check` finding now carries a public `CASP-<AREA>-<NNN>` code (e.g. `CASP-GIT-001`, `CASP-PROMPT-003`). `casp check --json` gains an **additive** `rule` field (no `schema_version` bump — still v1); `casp check` prints the code next to each WARN/FAIL. `casp rules` lists the catalogue (`--json` for data); `casp explain <CODE>` prints one rule's definition — what it verifies, the evidence it inspects, remediation — and also resolves an internal finding id. The registry (`src/rules.ts`) is the single source; a coverage test fails if any emitted finding id maps to no rule. Codes are a compatibility surface: they change only through explicit deprecation, never silently. Internal dot-namespaced `id` values are unchanged.
- **Security — injection-safe git for untrusted values.** `git()` is shell-based and must only take static, literal arguments; a new `gitArgs()` passes an argv array to `execFileSync` with **no shell**. Every call that interpolates untrusted input — `state.last_commit`, `sessions_dir` / `logs_dir`, CLI refs (`verify`, `state diff`) — now routes through `gitArgs()`, so a crafted value like `HEAD; rm -rf ~` becomes one invalid git argument (git errors → the check FAILs), never a shell command. Regression test included. The remaining literal-only `git()` calls carry no injection surface; migrating them is tracked defense-in-depth. New `docs/threat-model.md` records the trust boundaries.
- **New — published JSON Schemas.** `schemas/state.schema.json` and `schemas/check-result.schema.json` (Draft 2020-12), now shipped in the npm package. Structural conformance tests assert `casp init` produces every schema-required state key and `casp check --json` matches the result shape — so the schemas can't drift from the binary.
- **Docs — precise claim scoping.** New `docs/what-casp-proves.md` states exactly what a clean `casp check` proves (recorded execution state consistent with git evidence) and what it does **not** (code correctness, deployment, remote DB migrations, business intent). New `docs/rules.md` catalogues the rule codes. README updated: rule codes in the sample output, `rules`/`explain` in the command deck, links to the scope and threat-model docs.
- **Fixes (previously queued):** the installed pre-push hook runs under `set -eu` (POSIX; `pipefail` intentionally omitted as a `#!/bin/sh` bashism); `casp state diff` array deltas are now multiset-correct (dropping one of a duplicated element reports a single removal instead of cancelling out — a display-only path, never gating); a new advisory `CASP-MIGRATION-003` WARN fires when a configured `migrations_dir` holds migration files but `migrations_applied` is unset (silent for a genuinely empty dir).
- Eleven new regression tests (74 total): injection blocked; multiset delta; migrations-untracked WARN + empty-dir silence; `rules`/`explain` behavior; rule-coverage on clean and drifted states; both JSON schemas' structural conformance.

## 0.7.0 — 2026-06-17

- **New — `casp help` is first-class, plus per-command help.** `casp help` now prints the top-level overview and **exits 0** — previously the most natural thing a user types (`casp help`) hit the unknown-command path and exited 1. `casp help <command>` and `casp <command> --help` print a focused block for that verb: a one-line "why it exists", usage, every flag, and 1-2 real examples. Every verb is covered — `init`, `status`, `check`, `next`, `new`, `ship`, `close`, `install-hook`, `verify`, `state`, `help`. `casp <command> --help` and `casp help <command>` are byte-identical.
- **Discoverability — "how the loop works" in the top-level help.** The overview gains a short `THE LOOP` section (`init → status → (work) → check → ship/close → push`) — the mental model in four lines, with `check` named as the only hard gate.
- **Graceful unknown commands.** `casp help <bogus>` and `casp <bogus>` both print `no such command: <name>`, list the valid verbs, point at `casp help <command>`, and exit 1 — no stack trace, no full-help dump.
- **Single naming source.** Per-command help is rendered from one structured registry in `help.ts`; the valid-command list and the top-level command grouping read from the same canon, so the two surfaces can never name different verbs. No LLM, no network — a static, deterministic surface like the rest of the binary. CLI help stays usage-focused (one tight "what is CASP" paragraph + a pointer to casp.sh); the binary ships a frozen snapshot, the site stays current.
- Nine new regression tests: `casp help` exit 0 and identical to the no-arg/`-h`/`--help` block; the loop section present; `help check` is check-specific and `check --help` matches it; every verb has a focused block reachable both ways; `help <bogus>` and `<bogus>` exit 1 with the valid-command list; `-V`/`--version` still short-circuit.

## 0.6.0 — 2026-06-17

> **Behavior change — `casp next` now gates on drift.** `casp next` runs the validator before printing and **refuses to emit the next prompt when the state has drifted** (exit 1, drift summary on stderr, nothing on stdout). This is intentional: it closes the *start* boundary the same way `install-hook` closes the *push* boundary. If you script `casp next` and rely on it always printing, add `--no-check` to restore the old behavior. Both boundaries are now gated — you can neither start nor finish a session on a lying state.

- **New — `casp next` pre-session gate.** Before printing the next prompt, `casp next` runs the same in-process validator as `casp check` (`checkOne` — it never shells out to itself) and refuses on any FAIL: drift summary to stderr, stdout stays empty, exit 1. The missing-file and already-shipped cases keep their existing sharper messages; the gate catches the rest (stale `last_commit`, unverifiable claims, …). `--no-check` is the explicit escape hatch (you own the drift); `--no-git` skips the git-dependent checks. `next` stays a printer — it never runs anything after printing (anti-roadmap). Four new tests: clean prints, drift refuses with empty stdout, `--no-check` bypasses, missing-prompt keeps its specific guard.

- **New — `casp verify <commit>` + `casp state diff`.** Two read-only inspection verbs that make "git log is your compliance trail" actionable. `casp verify <commit>` materializes a commit in a throwaway detached worktree, runs the exact same validator there, propagates the verdict, and ALWAYS removes the worktree (the exit code is computed inside the try and applied after the finally, since `process.exit()` skips finally blocks — exiting mid-try would leak the worktree). `casp state diff [A] [B]` reports a field-level diff of `casp/state.json` between two commits (default `HEAD~1` → `HEAD`), with element-level deltas for array fields like `phases_shipped`; human output, or `--json`. Both never mutate the worktree, index, or history. Five new tests: state diff array/field deltas + no-op, verify clean (exit 0, no leaked worktree), verify a historically drifted commit (exit 1, still cleans up), verify a non-commit (exit 1).

- **New — `casp status --json`.** A machine-readable continuity snapshot: current/next phase, next prompt + its status, last commit/session, shipped/queued counts, git head/branch/ahead, and an **embedded validator verdict** computed in-process (the same `checkOne` `check` runs). Stable schema documented in [docs/status-json.md](docs/status-json.md) with the same stability promise as check-json. Crucially, `status --json` **never gates** — a valid cockpit always exits `0`, even when the embedded verdict is `drift` (reporting is not gating; that is `check`'s and `next`'s job). The structured session handoff, and the unit a future `status --all` fleet roll-up would aggregate (sketched, not built). Three new tests: shape + clean verdict, drift embedded but exit 0, human output intact.

- **New — `casp install-hook`.** One verb writes an executable `.git/hooks/pre-push` that runs `casp check --quiet`, turning "check before every push" from discipline into mechanism. This is the install-hook tier-1 item: a hands-off self-verifying loop auto-commits and pushes — it will not run `casp check` by hand — so wiring the deterministic state gate into `pre-push` is what makes CASP fire *inside* the autonomous loop instead of being a step the agent skips. The harness self-verifies that the *code* works; `casp check` on every push self-verifies that the *recorded state* still matches git, the one check the loop structurally can't do for itself.
- **Safe by construction.** The hook carries a `CASP-MANAGED-HOOK` marker. Installing refuses to clobber a pre-existing foreign `pre-push` hook unless `--force`; re-installing over CASP's own hook is idempotent (refresh, exit 0). `casp install-hook --remove` uninstalls only a hook CASP wrote, and refuses a foreign one. The hook resolution prefers `npx --no-install @justethales/casp` (the locally-installed package, no network) and falls back to a `casp` on PATH — probing with `--version` first so "package not resolvable" never gets mistaken for the check's own drift verdict.
- **Boundaries held.** Explicit opt-in only — `casp init` never installs the hook. CASP never writes `core.hooksPath` or any git config; when `core.hooksPath` is set (git ignores `.git/hooks` then), the verb refuses with a clear message rather than writing a silently-dead hook. Worktrees and `.git`-as-a-file resolve correctly (the path comes from `git rev-parse --git-path`).
- Eight new regression tests (42 total): hook installed + executable + marked; idempotent re-install; the installed hook run directly **blocks a drifted push (exit 1)** and **passes a clean one (exit 0)** via the documented `casp`-on-PATH fallback; foreign-hook refusal + `--force` override; `--remove` removes ours and refuses a stranger; `core.hooksPath` set → refuse without touching the config; linked-worktree resolution to the common hooks dir; outside a git repo → exit 1.

## 0.5.0 — 2026-06-16

- **New — configurable `sessions_dir` / `logs_dir`.** Two OPTIONAL `state.json` keys let a project point the validator at its real layout instead of adopting CASP's. Prompts still default to `docs/plan/sessions` and logs to `session-logs`; set either key and the *entire* protocol honors it — `casp check` (every claim, the shipped-history dirs, the state-bump surface, the uncommitted-changes pathspec), `casp new prompt|log`, `casp ship` (the wired `session_log` pointer), and `casp close` (newest-log detection). All messages print the **resolved** path, never the hardcoded default. Backward-compatible: a repo that sets neither key behaves exactly as before (every prior test stays green). Minor bump for new schema keys. Motivated by onboarding **a downstream project**, whose existing layout the hardcoded paths forced it to abandon.
- **Single resolver, one source of truth.** The three state-surface directories (sessions, logs, migrations) now resolve in one place (`resolveDirs` in `shared.ts`) — composed per-root so `check --all` honors each cockpit's own layout. `migrations_dir` keeps its existing no-default, opt-in semantics (a project with no migration concept reports none).
- **Out of scope, documented.** A single rolling next-prompt FILE (one `NEXT-SESSION-PROMPT.md` rather than a directory of per-session prompts) is **not** modeled — `next_prompt` already points at any file today, and `sessions_dir` governs the directory `check` scans and `new`/`ship` write into, not a single-file lifecycle. Modeling a rolling file is a different concept and was deliberately not built.
- Four new regression tests (34 total): custom-layout clean repo passes; a claim against a missing CUSTOM dir FAILs with the resolved name; shipped-history FAILs name the configured dirs; `new`/`ship`/`close` all write into the configured layout.

## 0.4.2 — 2026-06-16

- **Fix — `casp check --all <absolute path>` no longer doubles the path.** The optional root argument was `join`ed onto the current directory unconditionally, so an absolute root (`casp check --all /Users/me/projects`) became `<cwd>/Users/me/projects` and reported "no casp/ cockpit found." It now `resolve()`s the argument — an absolute root is used as-is; a relative root still resolves against the cwd; the no-arg (cwd) form is unchanged. Found within minutes of dogfooding `check --all` across the workspace for the fleet-gate launch. One new regression test (30 total).

## 0.4.1 — 2026-06-15

- **Fix — a fresh `casp init` is now green under `casp check` out of the box.** `init` scaffolded a `state.json` whose `next_prompt` pointed at `docs/plan/sessions/PHASE-1-FIRST-SLICE.md`, but never created that file — so the very first `casp check` a new user runs (step 5 of `init`'s own printed instructions) FAILed with "next_prompt points at a missing file." `init` now also scaffolds the `docs/plan/sessions/` and `session-logs/` directories and writes that first session prompt (from the canonical template, status `queued`). A brand-new repo now goes `init → status → check` with **0 FAIL**. Idempotent — an existing prompt is never overwritten. Found by onboarding a real project (a downstream project).

## 0.4.0 — 2026-06-15

- **New — `casp ship <slug>`.** The mechanical half of closing a session, automated: flips the phase's prompt to `status: shipped`, wires its `session_log` pointer, and moves the slug from `phases_queued` to `phases_shipped`. Resolves the slug against `docs/plan/sessions/*.md` by normalized name (so `0.4-close-loop` matches `PHASE-04-CLOSE-LOOP.md`), or takes `--prompt <path>`. Idempotent on re-run; refuses an unknown slug or a `pending` session-log id. **Touches no git** — the operator owns the commit.
- **New — `casp close`.** The guided deterministic close: auto-detects `last_commit` (HEAD) and `last_session_id` (newest session log), lets you confirm or override them (`--yes` / `--commit` / `--log` for non-interactive use), bumps `updated_at`, then runs `check` and exits with its verdict. `last_commit` is set to the current HEAD so the operator's own state-bump commit lands as the canonical close-loop PASS. **Never runs git** — no add, commit, or push. A state verb, not a harness.
- **New — `casp check --all [root]`.** Validates every `casp/` cockpit under a root in one report (per-cockpit verdict + aggregate), exits 1 if any cockpit drifts. `--all --json` emits a per-cockpit array under the existing schema. The fleet gate for "many agents, many repos." Internally, per-root validation was extracted into a pure `checkOne(root)` — single-root output and exit code are unchanged.
- **Change — migrations are now fully opt-in.** A project with no `migrations_dir` and no `migrations_applied` gets **no migration finding at all** (not even a PASS), so non-code cockpits (content, launch ops, research) carry zero migration noise. `casp init` no longer scaffolds `migrations_dir: "drizzle"`. A non-empty `migrations_applied` with no `migrations_dir` set is drift (FAIL): a claim with nothing to verify against. Existing repos that declare `migrations_dir` are unaffected.
- Ten new regression tests (27 total) covering ship, close (including the never-commits invariant), optional-migration silence, and `--all` aggregation.

## 0.3.2 — 2026-06-11

- **Docs only.** No code or CLI behavior change. Existing installs at 0.3.1 keep working unchanged.
- **The public roadmap is now the validated one.** Order by leverage: `install-hook` first (the gate stops being optional), then the pre-session gate on `casp next`, configurable paths, `status --json`, `verify <commit>` + `state diff`, and a demand-gated tail (binaries, narrow rollback, CI installer, webhook notifier). **`casp lint` is cut** — an LLM verb inside the CASP binary, even advisory, would undercut the deterministic claim; prose-vs-reality checking belongs in the agent reading `casp/`.

## 0.3.1 — 2026-06-10

- **Fix — Alembic (Python) migrations are now recognized.** The `migrations.match` check only counted `.sql` files, so a Python shop (`backend/alembic/versions/`) got a guaranteed false FAIL: every applied revision reported as missing from disk. The filter now accepts `.sql` and `.py`, ignoring dunder entries (`__init__.py`, `__pycache__`). Found within minutes of running 0.3.0 against a production FastAPI/Alembic repo (a production repo).
- **Fix — multi-log `session_log` values are supported (YAML list or comma-separated).** A phase shipped across several sessions legitimately lists all its logs in one frontmatter value (`session_log: [session-logs/a.md, session-logs/b.md]`). The validator treated the whole value as a single path and FAILed. Each entry is now resolved independently (repo-relative), and a FAIL names only the entries that are actually missing.
- Two new regression tests (17 total).

## 0.3.0 — 2026-06-10

- **Fix (verdict-changing) — no more false green when a claimed directory is missing.** A check that cannot find what it needs no longer silently skips: when `state.json` makes a claim that requires a directory and that directory is absent, `casp check` now **FAILs** with a `cannot verify <claim>` finding. Three claims are enforced: a real `last_session_id` requires `session-logs/` (`last_session.logs_dir`), a non-empty `migrations_applied` requires the migrations directory (`migrations.dir` — the canonical drift example itself was a false green when the dir was missing), and a non-empty `phases_shipped` requires both `docs/plan/sessions/` and `session-logs/` (`shipped_history.*`). Fresh-init placeholders and empty arrays are not claims and do not FAIL. **Repos that previously reported green may now correctly report drift — re-run `casp check` everywhere after upgrading.** Minor version bump for exactly that reason.
- **Fix — the canonical close loop no longer ends in a permanent WARN.** `last_commit` now reports **PASS** when it is the parent of HEAD and the HEAD commit touches only the state surface (`casp/`, `docs/plan/sessions/`, `session-logs/`) — the state-bump commit the protocol itself prescribes. Any other commit past `last_commit` stays WARN. `casp check` on CASP's own repo is now fully green (13 PASS · 0 WARN · 0 FAIL), previously a permanent explainable WARN.
- **Change — `last_session_id: "pending"` is now a WARN, not a FAIL.** Consistent with `last_commit: "pending"`: a fresh-init placeholder is not a verifiable claim. A fresh parked cockpit checks clean.
- **Hardening (from the two-auditor review).** An empty-string `last_session_id` now FAILs (`last_session.id_empty`) instead of silently skipping the check; every claim-backed path must be a real **directory** — a file squatting `session-logs/`, `docs/plan/sessions/` or the migrations dir reports a clean FAIL instead of crashing `readdirSync` (which also broke the `--json` always-valid guarantee).
- Nine new regression tests (15 total) covering the false-green class, placeholder semantics, state-bump recognition, and the file-squatting crash paths.


## 0.2.4 — 2026-06-10

- **New — `casp check --json`.** Machine-readable validator report: every check category as structured PASS/WARN/FAIL findings (stable `id`, `label`, `detail`, `fix`), plus `verdict`, `exit_code` and a `summary` block. The schema is documented in `docs/check-json.md` and versioned (`schema_version: 1`, bumps only on breaking changes). Strictly additive: the default human-readable output is unchanged, and the exit-code contract is identical — `--json` changes the *format* of the report, never the verdict. Even a missing or unparsable `casp/state.json` emits well-formed JSON (single `state.file` FAIL finding), so consumers never need a non-JSON fallback. Covered by four new tests in `npm test`.
- **Internal — version helper moved to `shared.ts`** (`pkgVersion()`), so both the CLI banner and the JSON report read the version from `package.json` at runtime. No behavior change.
- **Fix — `casp init` no longer scaffolds `.DS_Store`.** A Finder junk file sitting in a local clone's `templates/` was copied into the user's `casp/` directory (found by dogfooding `casp init` on the CASP repo itself; published npm installs were unaffected since `npm pack` strips `.DS_Store`).
- **CASP now manages itself.** The repo carries its own `casp/` cockpit (`state.json`, `now.md`, `roadmap.md`), session prompts and session logs — `casp check` runs green on the CASP repo before every push. Recursive validation: the tool that gates state drift is itself gated.

## 0.2.3 — 2026-06-10

- **Docs only.** No code or CLI behavior change. Existing installs at 0.2.2 keep working unchanged.
- **Repositioned the README and `package.json` description for the autonomous-model era.** The headline now leads with the deterministic, model-agnostic wedge — *the model holds the context; CASP proves the state is true, against git* — and frames CASP as the **complement** to long-running autonomous coding models (Claude Code today, any model next), not a competitor to their in-session memory. The prior "runs the whole roadmap / never loses the thread" line collided with how the new model generation now markets itself.

## 0.2.2 — 2026-06-09

- **Fix — `casp check` no longer FAILs a legitimately parked project.** `next_phase` and `next_prompt` may now be `null` (a project with no queued next slice — launch hold, roadmap complete, frozen). The keys must still be present, but an explicit `null` reports as PASS ("parked — no queued next slice") instead of a drift FAIL. Previously a parked `state.json` (e.g. a project in launch-mode hold) produced two false FAILs and blocked the push.
- **Fix — `casp --version` derives from `package.json` at runtime.** The version is no longer a hardcoded constant in `src/cli.ts` (which had silently desynced: the CLI reported `0.2.0` while npm shipped `0.2.1`). This is the same class of bug fixed by hand in 0.1.2 and regressed in 0.2.0; reading `package.json` removes the manual step for good.
- Exit-code contract still covered by `npm test` (clean → 0, drift → 1); both fixes verified green.

## 0.2.1 — 2026-06-09

- **Docs only.** No code or CLI behavior change. Existing installs at 0.2.0 keep working unchanged.
- **Repositioned the README and `package.json` description.** The headline now leads with the outcome — *your AI agent runs the whole project roadmap across sessions, writes its own next-session prompt, logs every session, and can't lose the thread* — with drift-detection kept as the trust guarantee rather than the pitch. The old "refuses to let your state lie" tagline was a previous agent's framing that buried the actual value.

## 0.2.0 — 2026-06-08

- **BREAKING — renamed Cockpit → CASP (Coding-Agent State Protocol).** The name "Cockpit" collided head-on with Red Hat Cockpit (cockpit-project.org); CASP relaunches the tool as a *protocol*, positioned the way MCP positioned itself.
- **npm package renamed `@justethales/cockpit` → `@justethales/casp`.** Install is now `npm i -g @justethales/casp` (the bare name `casp` is blocked by npm's name-similarity policy, so the package stays scoped; the CLI command is still `casp`). The old package is frozen; existing installs must reinstall under the new name.
- **CLI binary renamed `cockpit` → `casp`.** All verbs keep their names: `casp init | status | check | next | new prompt | new log`.
- **Slash-command `/cockpit` → `/casp`** (read-only status). `/next` is unchanged. The skills bundle directory moved `skills/cockpit/` → `skills/casp/`.
- **Scaffolded directory renamed `cockpit/` → `casp/`.** `casp init` now writes `casp/state.json`, `casp/now.md`, `casp/roadmap.md`, `casp/README.md`. Projects on the old layout should `git mv cockpit casp` once.
- **New verb `casp next`** — prints the next session's prompt straight from `state.next_prompt` (the CLI analogue of `/next`), pipe-friendly and non-zero when there is no actionable prompt.
- **`casp check` exit-code contract is now tested** (`npm test`): clean state → exit 0, drifted state → exit 1. The CI status check is real, not decorative.
- Repository moved to `github.com/ThalesGnimavo/casp`; homepage is `https://casp.sh`. Positioning, README and `package.json` description now lead with drift-detection / state-validation ("validates state against git — doesn't just store it"), not "memory".

## 0.1.2 — 2026-06-04

- **Docs only.** No code or CLI behavior change. Existing installs at 0.1.1 keep working unchanged.
- Repository migrated from `github.com/justethales/cockpit-skill` to `github.com/ThalesGnimavo/cockpit-skill` (GitHub username rename). All README badges, links, `package.json` repository/homepage/bugs, and source-file references updated.
- npm package name unchanged: still `@justethales/cockpit`. `npm i @justethales/cockpit` continues to work for all users.
- Fixed stale `VERSION = '0.1.0'` constant in `src/cli.ts` so `cockpit --version` now reports the correct release.
- Fixed dead `@thales/cockpit` reference still present in `templates/README.md` (the 0.1.1 cleanup missed this one).
- X/Twitter handle in README updated to `@ThalesGnimavo`.

## 0.1.1 — 2026-05-31

- **Docs only.** No code changes.
- README rewritten for clarity + npm/Google discoverability: stronger H1, badges, npm-friendly elevator pitch, agent compatibility matrix (Claude Code, Cursor, Aider, Continue), CLI reference table, citation block, public roadmap.
- `package.json` description rewritten and `keywords` expanded.
- Dropped the inflated "200-line discipline" claim from description and README — the validator alone is ~500 lines.
- Fixed the dead `@thales/cockpit` install commands left in the README. Everything now points at `@justethales/cockpit`.
- Bumped CHANGELOG to reflect that `check` ships 9 categories (the 0.1.0 entry said 7).

## 0.1.0 — 2026-05-30

Initial release.

- `cockpit init` — scaffolds a `cockpit/` directory with `state.json`, `now.md`, `roadmap.md`, `README.md`, and the three canonical templates (`session-prompt.md`, `session-log.md`, `audit-brief.md`).
- `cockpit check` — validator with 7 categories. Exits 1 on FAIL. ANSI output. `--quiet` flag for CI.
- `cockpit status` — one-screen snapshot. `--plain` flag strips ANSI.
- `cockpit new prompt --slug X` — copies the session-prompt template to `docs/plan/sessions/`.
- `cockpit new log --slug X` — copies the session-log template to `session-logs/`.
- Claude Code skills bundle (`skills/cockpit/`, `skills/next/`) — drop into `~/.claude/skills/` for instant `/cockpit` and `/next` slash commands.
